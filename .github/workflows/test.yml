name: test

on: push

jobs:
  npm-build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: nextjs-app

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install deps
        run: npm install

      - name: Build
        run: npm run build
